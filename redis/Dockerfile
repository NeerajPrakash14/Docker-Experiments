# Use the official Redis image from Docker Hub
FROM redis:latest

# Optionally, you can add configuration files or modify settings here
# For example, copying a custom Redis configuration file
# COPY redis.conf /usr/local/etc/redis/redis.conf

# Expose the default Redis port (6379) outside the container
EXPOSE 6379

# Start Redis when the container launches
CMD ["redis-server"]
